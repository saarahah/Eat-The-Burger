<h1>Burgers!</h1>

<h2>Burgers to eat</h2>
<ol>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ol>

<h2>Burgers that are devoured!</h2>
<ol>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ol>



<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" burger_name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Devour that bit</label><br>
    <input type="radio" burger_name="devoured" value="1" checked> Add Burger!<br>
    {{!-- <input type="radio" burger_name="devoured" value="0"> Awake --}}
  </div>

  <button type="submit">Add Burger</button>
</form>
